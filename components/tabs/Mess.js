import { Text,View } from 'react-native'

const Mess = () =>{
    return (
      <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
        <Text>Mess!</Text>
      </View>
    );
  }
export default Mess