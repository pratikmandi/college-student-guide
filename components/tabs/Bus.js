import { Text,View } from 'react-native'

const Bus = () =>{
    return (
      <View style={{ flex: 1, justifyContent: 'center', alignItems: 'center' }}>
        <Text>Bus Timing!</Text>
      </View>
    );
  }
export default Bus